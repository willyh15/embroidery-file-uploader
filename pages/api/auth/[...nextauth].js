// pages/api/auth/[...nextauth].js (DISABLED)

// Auth temporarily disabled - this file can be deleted or left as-is without being used.

export default function handler(req, res) {
  return res.status(200).json({ message: "Auth temporarily disabled" });
}
